apiVersion: v1
kind: Service
metadata:
  name: lm-studio-gateway-svc
  namespace: spiffe-research
  labels:
    app: lm-studio-gateway
spec:
  type: ClusterIP
  selector:
    app: lm-studio-gateway
  ports:
  - name: http
    port: 8446
    targetPort: 8446
    protocol: TCP
